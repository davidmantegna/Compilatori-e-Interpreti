let
    bool fatt(int n)
        if (n <= 1) then{
            1
        }else{
            n * fatt(n-1)
        }
    ;
in
    fatt(5);

// Errore: "Tipo incompatibile per Times. È richiesto un intero."